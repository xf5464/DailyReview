# Repository instructions

- 添加或调整任何数据指标时，必须优先在 FRED 检查是否存在口径匹配的序列；若存在，应优先使用 FRED。只有在 FRED 不提供该指标，或其更新时效、历史覆盖、数据口径无法满足需求时，才使用其他来源，并在系统设计文档中说明原因与回退策略。
- 新增或调整总览页小图表时，必须遵循 `docs/chart-display-rules.md`：季度频率小图统一读取配置并显示最近 12 个季度；其余日度、周度、月度普通折线小图统一读取配置并显示最近 12 个月。不得针对具体图表 ID 写死窗口；横轴使用紧凑月份或季度标签并省略不必要的年份。新增图表后必须分别核对日度、周度、月度、季度代表图的总览显示。
- 折线图展示的时间跨度大于 3 年时，X 轴必须竖排标注年份；默认每隔一年标注一次，无需逐年标注。
- 所有曲线图必须提供鼠标悬停提示（tips），至少显示当前点的日期和数值；多曲线图应在同一提示中区分并显示各条曲线对应的数据。
- NDX 距历史最高点回撤曲线的鼠标悬停提示（tips）必须同时显示截至当前点的历史最高点日期和具体点位。
- 预测条件相关功能必须遵循 `docs/forecast-condition-rules.md`：`条件数据列表` 必须始终展示系统中的全部预测条件；新增、删除或重命名预测条件时，必须同步预测主界面、条件数据列表、GitHub Actions 自动检查/通知逻辑和测试。条件数据列表每次打开时所有条件默认不勾选，必须由用户主动选择后执行。
